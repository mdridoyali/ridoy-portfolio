
const Title = ({heading}) => {
    return (
        <div>
            <h1 className='text-3xl uppercase font-bold border-b-2 border-lime-500 mb-8 w-max mx-auto'>{heading}</h1>
        </div>
    );
};

export default Title;
